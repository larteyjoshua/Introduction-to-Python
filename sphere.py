import math
radius= float(input("Enter the radius of the sphere "))
suface_area = 4*math.pi* radius*radius
volume = (4/3)*math.pi*radius*radius*radius
print ("Surface Area="+ str (suface_area))
print ("Volume= " + str(volume))